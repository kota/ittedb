<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  カテゴリ:<%= f.text_field :category_id %><br/>
  名前:<%= f.text_field :name %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
